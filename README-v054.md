# Scoutly v0.5.4 — Edge-case cleanup + LEGO expansion

Apply this over the current Scoutly project, then commit and push.

```bash
cd ~/Scoutly
git add -A
git commit -m "Add edge-case cleanup and LEGO expansion"
git push
```

Validation run in this sandbox:

```text
Backend tests: 98 passed
Frontend build: passed
```
