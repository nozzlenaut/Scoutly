NEXT_PUBLIC_API_URL=http://localhost:8000
# Optional server-only override used by Vercel proxy routes such as /admin/prices.
# In production this can point to the same public Railway backend URL.
API_URL=http://localhost:8000
