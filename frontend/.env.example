NEXT_PUBLIC_API_URL=http://localhost:8000
# Optional server-only override used by frontend proxy routes such as /admin/prices.
# In production this can point to the internal backend service URL.
API_URL=http://localhost:8000
