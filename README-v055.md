# Scoutly v0.5.5 — Affiliate label + LEGO expansion

Apply this over the current Scoutly project, then commit and push.

```bash
cd ~/Scoutly
git add -A
git commit -m "Update affiliate label and expand LEGO catalog"
git push
```

Validation run in this sandbox:

```text
Backend tests: see final response
Frontend build: see final response
```
